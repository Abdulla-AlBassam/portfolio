---
import BaseLayout from "@/layouts/BaseLayout.astro";
import fs from "node:fs";
import path from "node:path";

function scanProjectDir(dirName: string) {
  const dirPath = path.join(process.cwd(), "public", "projects", dirName);

  let files: string[] = [];
  try {
    files = fs.readdirSync(dirPath);
  } catch {
    files = [];
  }

  const images = files
    .filter((f) => /\.(png|jpg|jpeg|gif|webp)$/i.test(f))
    .sort();
  const code = files.filter((f) => /\.(php)$/i.test(f)).sort();
  const databases = files
    .filter((f) => /\.(sqlite|sqlite3|db)$/i.test(f))
    .sort();

  return {
    images: images.map((f) => `/projects/${dirName}/${f}`),
    code: code.map((f) => ({
      name: f,
      path: `/projects/${dirName}/${f}`,
    })),
    databases: databases.map((f) => ({
      name: f,
      path: `/projects/${dirName}/${f}`,
    })),
  };
}

const restApi = scanProjectDir("rest-api");
const blueprint = scanProjectDir("architecture-blueprint");
---

<BaseLayout title="REST API + Architecture Blueprint — Abdulla AlBassam">
  <div class="page">
    <a href="/showcase/projects" class="back-link">&larr; Back to Projects</a>

    <div class="page-header">
      <h1>REST API Build + Architecture Blueprint</h1>
      <p class="page-desc">
        Object-oriented PHP REST API with SQLite backend, and a future-proof
        architecture specification for multi-conference scaling.
      </p>
    </div>

    <section class="project-section">
      <h2>REST API</h2>

      {restApi.images.length > 0 && (
        <div class="section-group">
          <h3>Screenshots</h3>
          <div class="image-gallery">
            {restApi.images.map((src) => (
              <a href={src} target="_blank" rel="noopener noreferrer" class="gallery-item">
                <img src={src} alt="" loading="lazy" />
              </a>
            ))}
          </div>
        </div>
      )}

      {restApi.code.length > 0 && (
        <div class="section-group">
          <h3>Source Files</h3>
          <div class="file-list">
            {restApi.code.map((file) => (
              <a href={file.path} target="_blank" rel="noopener noreferrer" class="file-item">
                <span class="file-ext">.php</span>
                <span class="file-name">{file.name}</span>
              </a>
            ))}
          </div>
        </div>
      )}

      {restApi.databases.length > 0 && (
        <div class="section-group">
          <h3>Database Files</h3>
          <div class="file-list">
            {restApi.databases.map((file) => (
              <a href={file.path} target="_blank" rel="noopener noreferrer" class="file-item">
                <span class="file-ext">.db</span>
                <span class="file-name">{file.name}</span>
              </a>
            ))}
          </div>
        </div>
      )}

      {restApi.images.length === 0 && restApi.code.length === 0 && restApi.databases.length === 0 && (
        <p class="empty-state">No files yet.</p>
      )}
    </section>

    <section class="project-section">
      <h2>Architecture Blueprint</h2>

      {blueprint.images.length > 0 && (
        <div class="section-group">
          <h3>Diagrams</h3>
          <div class="image-gallery">
            {blueprint.images.map((src) => (
              <a href={src} target="_blank" rel="noopener noreferrer" class="gallery-item">
                <img src={src} alt="" loading="lazy" />
              </a>
            ))}
          </div>
        </div>
      )}

      {blueprint.code.length > 0 && (
        <div class="section-group">
          <h3>Source Files</h3>
          <div class="file-list">
            {blueprint.code.map((file) => (
              <a href={file.path} target="_blank" rel="noopener noreferrer" class="file-item">
                <span class="file-ext">.php</span>
                <span class="file-name">{file.name}</span>
              </a>
            ))}
          </div>
        </div>
      )}

      {blueprint.databases.length > 0 && (
        <div class="section-group">
          <h3>Database Files</h3>
          <div class="file-list">
            {blueprint.databases.map((file) => (
              <a href={file.path} target="_blank" rel="noopener noreferrer" class="file-item">
                <span class="file-ext">.db</span>
                <span class="file-name">{file.name}</span>
              </a>
            ))}
          </div>
        </div>
      )}

      {blueprint.images.length === 0 && blueprint.code.length === 0 && blueprint.databases.length === 0 && (
        <p class="empty-state">No files yet.</p>
      )}
    </section>
  </div>
</BaseLayout>

<style>
  .page {
    min-height: 100vh;
    padding: 8rem 2rem 4rem;
    max-width: 1000px;
    margin: 0 auto;
  }

  .back-link {
    display: inline-block;
    font-size: 0.85rem;
    color: var(--text-muted);
    text-decoration: none;
    transition: color 0.2s;
    margin-bottom: 2rem;
  }

  .back-link:hover {
    color: white;
  }

  .page-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .page-header h1 {
    font-family: "Space Grotesk", sans-serif;
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 700;
    letter-spacing: -0.03em;
    color: white;
    margin: 0;
  }

  .page-desc {
    color: var(--text-muted);
    margin-top: 1rem;
    font-size: 0.95rem;
    line-height: 1.6;
  }

  .project-section {
    margin-bottom: 4rem;
  }

  .project-section h2 {
    font-family: "Space Grotesk", sans-serif;
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin: 0 0 2rem 0;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--border);
  }

  .section-group {
    margin-bottom: 2.5rem;
  }

  .section-group h3 {
    font-family: "Space Grotesk", sans-serif;
    font-size: 1.1rem;
    font-weight: 600;
    color: white;
    margin: 0 0 1.25rem 0;
  }

  .image-gallery {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
    gap: 1rem;
  }

  .gallery-item {
    display: block;
    border: 1px solid var(--border);
    border-radius: 0.75rem;
    overflow: hidden;
    transition:
      border-color 0.3s,
      transform 0.3s;
  }

  .gallery-item:hover {
    border-color: var(--border-hover);
    transform: scale(1.02);
  }

  .gallery-item img {
    width: 100%;
    height: auto;
    display: block;
  }

  .file-list {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .file-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.85rem 1.25rem;
    border: 1px solid var(--border);
    border-radius: 0.75rem;
    color: var(--text-muted);
    text-decoration: none;
    font-size: 0.85rem;
    transition:
      border-color 0.3s,
      background 0.3s,
      color 0.2s;
  }

  .file-item:hover {
    border-color: var(--border-hover);
    background: var(--bg-hover);
    color: white;
  }

  .file-ext {
    font-family: "Space Grotesk", sans-serif;
    font-size: 0.65rem;
    font-weight: 600;
    text-transform: uppercase;
    color: var(--text-muted);
    background: rgba(255, 255, 255, 0.03);
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 0.2rem 0.5rem;
    flex-shrink: 0;
  }

  .file-name {
    font-family: "Space Grotesk", sans-serif;
    font-weight: 500;
  }

  .empty-state {
    color: var(--text-muted);
    font-size: 0.85rem;
    font-style: italic;
    padding: 2rem;
    border: 1px dashed var(--border);
    border-radius: 0.75rem;
    text-align: center;
  }

  @media (max-width: 768px) {
    .page {
      padding: 7rem 1.5rem 3rem;
    }

    .image-gallery {
      grid-template-columns: 1fr;
    }
  }
</style>
